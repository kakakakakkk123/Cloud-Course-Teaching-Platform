<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.EduCourseCategoryMapper">

    <resultMap id="EduCourseCategoryResult" type="com.ruoyi.system.domain.course.EduCourseCategory">
        <id property="categoryId" column="category_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="ancestors" column="ancestors"/>
        <result property="categoryName" column="category_name"/>
        <result property="orderNum" column="order_num"/>
        <result property="icon" column="icon"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectEduCourseCategoryVo">
        select category_id, parent_id, ancestors, category_name, order_num, icon, status,
               del_flag, create_by, create_time, update_by, update_time, remark
        from edu_course_category
    </sql>

    <select id="selectEduCourseCategoryById" parameterType="Long" resultMap="EduCourseCategoryResult">
        <include refid="selectEduCourseCategoryVo"/>
        where category_id = #{categoryId}
    </select>

    <select id="selectEduCourseCategoryList" parameterType="com.ruoyi.system.domain.course.EduCourseCategory" resultMap="EduCourseCategoryResult">
        <include refid="selectEduCourseCategoryVo"/>
        <where>
            del_flag = '0'
            <if test="categoryName != null and categoryName != ''">
                and category_name like concat('%', #{categoryName}, '%')
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="parentId != null">
                and parent_id = #{parentId}
            </if>
        </where>
        order by order_num asc, category_id asc
    </select>

    <insert id="insertEduCourseCategory" parameterType="com.ruoyi.system.domain.course.EduCourseCategory" useGeneratedKeys="true" keyProperty="categoryId">
        insert into edu_course_category (
            parent_id, ancestors, category_name, order_num, icon, status, del_flag,
            create_by, create_time, remark
        ) values (
            #{parentId}, #{ancestors}, #{categoryName}, #{orderNum}, #{icon}, #{status}, '0',
            #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateEduCourseCategory" parameterType="com.ruoyi.system.domain.course.EduCourseCategory">
        update edu_course_category
        <set>
            parent_id = #{parentId},
            ancestors = #{ancestors},
            category_name = #{categoryName},
            order_num = #{orderNum},
            icon = #{icon},
            status = #{status},
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where category_id = #{categoryId}
    </update>

    <update id="deleteEduCourseCategoryById" parameterType="Long">
        update edu_course_category
        set del_flag = '2', update_time = sysdate()
        where category_id = #{categoryId}
    </update>

    <update id="deleteEduCourseCategoryByIds" parameterType="Long">
        update edu_course_category
        set del_flag = '2', update_time = sysdate()
        where category_id in
        <foreach collection="array" item="categoryId" open="(" separator="," close=")">
            #{categoryId}
        </foreach>
    </update>

    <select id="hasChildByCategoryId" parameterType="Long" resultType="int">
        select count(1)
        from edu_course_category
        where parent_id = #{categoryId}
          and del_flag = '0'
    </select>

    <select id="countCourseByCategoryId" parameterType="Long" resultType="int">
        select count(1)
        from edu_course
        where category_id = #{categoryId}
          and del_flag = '0'
    </select>

</mapper>
